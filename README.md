# Fake_vs_Real_Image_Classifier
* This is a classifier algorithm that makes use of Convolution Neural Network (CNN) with 3 convolution layers, a max pooling layer, a flattening layer, one hidden network layer, and one dropout layer.
